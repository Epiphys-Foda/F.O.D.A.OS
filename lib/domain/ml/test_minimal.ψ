import "../../core/stdlib/gf65537.ψ"

let test_gf_add() =
    gf_add(3, 5)

let test_gf_mul() =
    gf_mul(3, 5)

let test_gf_inv() =
    gf_inv(7)

let test_array() =
    let arr = [1, 2, 3]
    arr[0] + arr[1] + arr[2]

let test_loop() =
    let s = 0
    let i = 0
    for i < 5
        s = s + i
        i = i + 1
    s

let main() =
    let t1 = test_gf_add()
    let t2 = test_gf_mul()
    let t3 = test_gf_inv()
    let t4 = test_array()
    let t5 = test_loop()
    t1 + t2 + t3 + t4 + t5
