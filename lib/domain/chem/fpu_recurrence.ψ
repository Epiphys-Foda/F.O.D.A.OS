import "ck.ψ"
let bad = [0]

let erec = 880
bad[0] = bad[0] + ck(erec, 850, 950)

let eeq = 15
let refusal = erec / eeq
bad[0] = bad[0] + ck_lo(refusal, 50)

let s_in = 7
let s_out = 7
let sq = s_out - s_in
bad[0] = bad[0] + ck(sq, 0, 0)

let v1 = 2
let v2 = 4
let vr = v2 / v1
bad[0] = bad[0] + ck(vr, 2, 2)

if 1955 < 1953
    bad[0] = bad[0] + 1
if 1955 > 1957
    bad[0] = bad[0] + 1
if 1965 < 1963
    bad[0] = bad[0] + 1
if 1965 > 1967
    bad[0] = bad[0] + 1
if 1967 < 1965
    bad[0] = bad[0] + 1
if 1967 > 1969
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
