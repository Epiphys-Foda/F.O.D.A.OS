import "ck.ψ"
let bad = [0]

let r0 = 50
let rmin = 1
let cr = r0 / rmin
bad[0] = bad[0] + ck_lo(cr, 40)

let tr = 42
bad[0] = bad[0] + ck(tr, 35, 50)

let t10k = 10000
bad[0] = bad[0] + ck(t10k, 8000, 20000)

let fd = 40
bad[0] = bad[0] + ck(fd, 30, 200)

let pulse = 1
bad[0] = bad[0] + ck_lo(pulse, 1)

if 1934 < 1932
    bad[0] = bad[0] + 1
if 1934 > 1936
    bad[0] = bad[0] + 1
if 1990 < 1988
    bad[0] = bad[0] + 1
if 1990 > 1992
    bad[0] = bad[0] + 1
if 2000 < 1998
    bad[0] = bad[0] + 1
if 2000 > 2002
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
