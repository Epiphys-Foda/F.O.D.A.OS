import "ck.ψ"
let bad = [0]

let n_luc = 1
let n_atp = 1
let n_o2 = 1
let n_ph = 1
let bal = n_luc + n_atp + n_o2 - 3 * n_ph
bad[0] = bad[0] + ck(bal, 0, 0)

let qy = 88
bad[0] = bad[0] + ck(qy, 80, 95)

let cff = 560
bad[0] = bad[0] + ck(cff, 550, 570)

let cds = 475
bad[0] = bad[0] + ck(cds, 470, 480)

let counter = 1
bad[0] = bad[0] + ck_lo(counter, 1)

let flash10 = 10
bad[0] = bad[0] + ck(flash10, 3, 20)

if 1887 < 1885
    bad[0] = bad[0] + 1
if 1887 > 1889
    bad[0] = bad[0] + 1
if 1957 < 1955
    bad[0] = bad[0] + 1
if 1957 > 1959
    bad[0] = bad[0] + 1
if 2008 < 2006
    bad[0] = bad[0] + 1
if 2008 > 2010
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
