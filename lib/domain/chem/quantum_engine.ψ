import "ck.ψ"
let bad = [0]

let nuh = 10
let nuc = 5
let eta_m = nuc * 100 / nuh
bad[0] = bad[0] + ck(eta_m, 48, 52)

let th = 400
let tc = 200
let eta_c = tc * 100 / th
bad[0] = bad[0] + ck(eta_c, 48, 52)

let dz = eta_m - eta_c
bad[0] = bad[0] + ck(dz, 0, 0)

let win = 12 * tc - nuc * th
bad[0] = bad[0] + ck_lo(win, 1)

let eta_f = 40
let dfr = eta_c - eta_f
bad[0] = bad[0] + ck_lo(dfr, 1)

if 1959 < 1957
    bad[0] = bad[0] + 1
if 1959 > 1961
    bad[0] = bad[0] + 1
if 2004 < 2002
    bad[0] = bad[0] + 1
if 2004 > 2006
    bad[0] = bad[0] + 1
if 2016 < 2014
    bad[0] = bad[0] + 1
if 2016 > 2018
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
