import "ck.ψ"
let am = [
    1008, 12011, 14007, 15999, 22990, 35453,
]

let mols = [
    2, 0, 0, 1, 0, 0, 18015, 0, 1, 0, 2, 0, 0, 44009,
    4, 1, 0, 0, 0, 0, 16043, 3, 0, 1, 0, 0, 0, 17031,
    12, 6, 0, 6, 0, 0, 180156, 5, 2, 1, 2, 0, 0, 75067,
    6, 6, 0, 0, 0, 0, 78114, 0, 0, 0, 0, 1, 1, 58443,
]

let bonds = [
    107, 109, 152, 154, 134, 134, 120, 120, 97, 96, 102, 101,
    142, 143, 124, 121, 114, 116, 179, 177, 133, 127,
]

let ens = [
    220, 220, 0, 255, 220, 0, 344, 220, 1, 316, 220, 1,
    344, 255, 1, 93, 316, 2, 220, 398, 2, 79, 398, 2,
]

let seq = [
    1, 2, 3, 2, 4, 3, 4, 2, 1, 1, 2, 3, 4, 3, 2,
]

let dna_res = [
    31321, 30420, 32921, 28918,
]

let rna_res = [
    32920, 30620, 34520, 30520,
]

let res20 = [
    57052, 71079, 87078, 97117, 99133, 101105, 103139, 113159, 113159, 114104,
    115088, 128130, 128174, 129116, 131192, 137141, 147177, 156188, 163176, 186213,
]

let degen = [
    2, 6, 3, 1, 4, 6, 4, 4, 4, 2,
    2, 2, 2, 2, 2, 2, 2, 1, 6, 4,
]

let bad = [0]

let i = 0
let m1f = [0]
for i < 8
    let b = i * 7
    let mh = mols[b]
    let mc = mols[b + 1]
    let mn = mols[b + 2]
    let mo = mols[b + 3]
    let mna = mols[b + 4]
    let mcl = mols[b + 5]
    let mass = mh * am[0] + mc * am[1] + mn * am[2] + mo * am[3] + mna * am[4] + mcl * am[5]
    let lit = mols[b + 6]
    let dd = mass - lit
    bad[0] = bad[0] + dd * dd
    i = i + 1

let i = 0
let m2f = [0]
for i < 11
    let b = i * 2
    let pr = bonds[b]
    let li = bonds[b + 1]
    let d = pr - li
    let ad =
        if d < 0
            0 - d
        else
            d
    let t = 20 * ad
    if t > li
        m2f[0] = m2f[0] + 1
    i = i + 1
let d2 = m2f[0] - 0
bad[0] = bad[0] + d2 * d2

let i = 0
let m3f = [0]
for i < 8
    let b = i * 3
    let e1 = ens[b]
    let e2 = ens[b + 1]
    let ec = ens[b + 2]
    let d = e1 - e2
    let ad =
        if d < 0
            0 - d
        else
            d
    let cls = 2
    if ad < 170
        cls = 1
    if ad < 40
        cls = 0
    let dd = cls - ec
    bad[0] = bad[0] + dd * dd
    i = i + 1

let dA = dna_res[0]
let dT = dna_res[1]
let dG = dna_res[2]
let dC = dna_res[3]
let rA = rna_res[0]
let rU = rna_res[1]
let rG = rna_res[2]
let rC = rna_res[3]
let q1 = rA - dA - 1600
let q2 = rC - dC - 1600
let q3 = rG - dG - 1600
let q4 = rU + 1403 - 1600 - dT
let a1 =
    if q1 < 0
        0 - q1
    else
        q1
let a2 =
    if q2 < 0
        0 - q2
    else
        q2
let a3 =
    if q3 < 0
        0 - q3
    else
        q3
let a4 =
    if q4 < 0
        0 - q4
    else
        q4
let s1 = a1 - 5
let s2 = a2 - 5
let s3 = a3 - 5
let s4 = a4 - 5
bad[0] = bad[0] + ck_hi(a1, 5)
bad[0] = bad[0] + ck_hi(a2, 5)
bad[0] = bad[0] + ck_hi(a3, 5)
bad[0] = bad[0] + ck_hi(a4, 5)

let i = 0
let cA = [0]
let cT = [0]
let cG = [0]
let cC = [0]
for i < 15
    let ix = i * 1
    let b = seq[ix]
    if b - 1 < 1
        cA[0] = cA[0] + 1
    if b - 2 < 1
        if 2 - b < 1
            cT[0] = cT[0] + 1
    if b - 3 < 1
        if 3 - b < 1
            cG[0] = cG[0] + 1
    if b - 4 < 1
        if 4 - b < 1
            cC[0] = cC[0] + 1
    i = i + 1
let at = cA[0] + cT[0]
let gc = cG[0] + cC[0]
let tm = 2 * at + 4 * gc
let dt = tm - 44
bad[0] = bad[0] + dt * dt
let hb = 2 * at + 3 * gc
let dh = hb - 37
bad[0] = bad[0] + dh * dh
let chA = cA[0] + cT[0]
let chT = cT[0] + cA[0]
let dc = chA - chT
bad[0] = bad[0] + dc * dc

let rise = 340
let pitch = 10 * rise
let dph = pitch - 3400
bad[0] = bad[0] + dph * dph

let NA15 = 602214076 * 1000000000000000
let dip = 2 * 3100000000 * 660 * 1000000000000000
let lo = 6400 * NA15
let hi = 7200 * NA15
let r1 = dip - lo
let r2 = hi - dip
bad[0] = bad[0] + ck_lo(r1, 0)
bad[0] = bad[0] + ck_lo(r2, 0)

let p1 = res20[0] + res20[0] + 18015 - 132118
let p2 = res20[1] + res20[0] + 18015 - 146146
let p3 = res20[0] + res20[0] + res20[0] + 18015 - 189170
let p4 = res20[7] + res20[1] + 18015 - 202250
if p1 < 0 - 5
    bad[0] = bad[0] + 1
bad[0] = bad[0] + ck_hi(p1, 5)
if p2 < 0 - 5
    bad[0] = bad[0] + 1
bad[0] = bad[0] + ck_hi(p2, 5)
if p3 < 0 - 5
    bad[0] = bad[0] + 1
bad[0] = bad[0] + ck_hi(p3, 5)
if p4 < 0 - 5
    bad[0] = bad[0] + 1
bad[0] = bad[0] + ck_hi(p4, 5)

let rs = rA + rU + rG + rC
let drs = rs - 128580
bad[0] = bad[0] + drs * drs
let dgsum = 2 + 6 + 3 + 1 + 4 + 6 + 4 + 4 + 4 + 2 + 2 + 2 + 2 + 2 + 2 + 2 + 2 + 1 + 6 + 4
let ddg = dgsum - 61
bad[0] = bad[0] + ddg * ddg
let cod = 4 * 4 * 4
let dcd = cod - 64
bad[0] = bad[0] + dcd * dcd
let sense = cod - 3 - 61
bad[0] = bad[0] + sense * sense
let pe = 2 * 12011 + 4 * 1008
let dpe = pe - 28054
bad[0] = bad[0] + dpe * dpe
let cel = 180156 - 18015 - 162141
bad[0] = bad[0] + cel * cel
let pe1000 = 28054 * 1000
let dpe2 = pe1000 - 28054000
bad[0] = bad[0] + dpe2 * dpe2

arr_get(arr_new_n(1), bad[0])
