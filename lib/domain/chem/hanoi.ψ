import "ck.ψ"
let bad = [0]

let h1 = 2 * 1 - 1
let dz1 = h1 - 1
bad[0] = bad[0] + ck(dz1, 0, 0)

let h2 = 2 * h1 + 1
let dz2 = h2 - 3
bad[0] = bad[0] + ck(dz2, 0, 0)
let h3 = 2 * h2 + 1
let dz3 = h3 - 7
bad[0] = bad[0] + ck(dz3, 0, 0)
let h4 = 2 * h3 + 1
let dz4 = h4 - 15
bad[0] = bad[0] + ck(dz4, 0, 0)
let h5 = 2 * h4 + 1
let dz5 = h5 - 31
bad[0] = bad[0] + ck(dz5, 0, 0)

let h10 = 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 - 1
let dz6 = h10 - 1023
bad[0] = bad[0] + ck(dz6, 0, 0)

let n_legend = 64
bad[0] = bad[0] + ck_lo(n_legend, 64)

if 1883 < 1881
    bad[0] = bad[0] + 1
if 1883 > 1885
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
