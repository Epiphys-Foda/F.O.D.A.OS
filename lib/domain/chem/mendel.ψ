import "ck.ψ"
let bad = [0]

let rec_f1 = 0
bad[0] = bad[0] + ck_hi(rec_f1, 0)

let cells1 = 2 * 2
let dz1 = cells1 - 4
bad[0] = bad[0] + ck(dz1, 0, 0)
let geno = 1 + 2 + 1
let dz2 = geno - 4
bad[0] = bad[0] + ck(dz2, 0, 0)
let pheno = 3 + 1
let dz3 = pheno - 4
bad[0] = bad[0] + ck(dz3, 0, 0)

let cells2 = 4 * 4
let dz4 = cells2 - 16
bad[0] = bad[0] + ck(dz4, 0, 0)
let classes = 9 + 3 + 3 + 1
let dz5 = classes - 16
bad[0] = bad[0] + ck(dz5, 0, 0)

let n_round = 5474
let n_wrink = 1850
let total = n_round + n_wrink
let dz6 = total - 7324
bad[0] = bad[0] + ck(dz6, 0, 0)

let exp_dom = total * 3 / 4
let dz7 = exp_dom - 5493
bad[0] = bad[0] + ck(dz7, 0, 0)
let exp_rec = total / 4
let dz8 = exp_rec - 1831
bad[0] = bad[0] + ck(dz8, 0, 0)
let dz9 = exp_dom + exp_rec - total
bad[0] = bad[0] + ck(dz9, 0, 0)

let ratio = n_round * 100 / n_wrink
let dz10 = ratio - 295
bad[0] = bad[0] + ck(dz10, 0, 0)
bad[0] = bad[0] + ck(ratio, 290, 300)

let dy_ry = 315
let dy_rg = 108
let dy_wy = 101
let dy_wg = 32
let total2 = dy_ry + dy_rg + dy_wy + dy_wg
let dz11 = total2 - 556
bad[0] = bad[0] + ck(dz11, 0, 0)
let exp_9 = total2 * 9 / 16
let dz12 = exp_9 - 312
bad[0] = bad[0] + ck(dz12, 0, 0)
let exp_1 = total2 / 16
let dz13 = exp_1 - 34
bad[0] = bad[0] + ck(dz13, 0, 0)

if 1866 < 1864
    bad[0] = bad[0] + 1
if 1866 > 1868
    bad[0] = bad[0] + 1
if 1900 < 1898
    bad[0] = bad[0] + 1
if 1900 > 1902
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
