import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let moire = 2460 / 19
bad[0] = bad[0] + ck(moire, 120, 140)

let amp = 1290 / 25
bad[0] = bad[0] + ck(amp, 48, 56)

let wide = 5000
let flat = 10
let flatr = wide / flat
bad[0] = bad[0] + ck(flatr, 400, 600)

let phases = 3
bad[0] = bad[0] + ck_lo(phases, 3)

if 2011 < 2009
    bad[0] = bad[0] + 1
if 2011 > 2013
    bad[0] = bad[0] + 1
if 2018 < 2016
    bad[0] = bad[0] + 1
if 2018 > 2020
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
