import "ck.ψ"
let bad = [0]

let z = 4
bad[0] = bad[0] + ck(z, 4, 4)

let tc_mf = 4
bad[0] = bad[0] + ck_lo(tc_mf, 4)

let t_ratio = 227
bad[0] = bad[0] + ck(t_ratio, 225, 229)

let beta_2d = 1
bad[0] = bad[0] + ck_lo(beta_2d, 0)

if 1944 < 1942
    bad[0] = bad[0] + 1
if 1944 > 1946
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
