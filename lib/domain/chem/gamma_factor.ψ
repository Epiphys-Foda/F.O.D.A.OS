import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let g1 = 1083
bad[0] = bad[0] + ck(g1, 1070, 1095)

let g2 = 1133
bad[0] = bad[0] + ck(g2, 1120, 1145)

let g3 = 1250
bad[0] = bad[0] + ck(g3, 1240, 1260)

bad[0] = bad[0] + ck_hi(g1, g2)
bad[0] = bad[0] + ck_hi(g2, g3)

let ship = 12
let earth = 13
bad[0] = bad[0] + ck_hi(ship, earth)

if 1905 < 1903
    bad[0] = bad[0] + 1
if 1905 > 1907
    bad[0] = bad[0] + 1
if 1892 < 1890
    bad[0] = bad[0] + 1
if 1892 > 1894
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
