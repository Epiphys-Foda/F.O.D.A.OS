import "ck.ψ"
let bad = [0]

let d_t = 0
let dz1 = d_t
bad[0] = bad[0] + ck(dz1, 0, 0)

let l_f = 334
let c_w = 4
let ratio = 80
bad[0] = bad[0] + ck(ratio, 78, 82)
bad[0] = bad[0] + ck_hi(ratio, l_f)

let l_v = 2260
if l_v < l_f * 6
    bad[0] = bad[0] + 1
if l_v > l_f * 8
    bad[0] = bad[0] + 1
bad[0] = bad[0] + ck_lo(l_v, l_f)

let sweat = 2426
bad[0] = bad[0] + ck(sweat, 2300, 2500)

let q_total = 418
let dz2 = q_total - l_f - 84
bad[0] = bad[0] + ck(dz2, 0, 0)

if 1762 < 1760
    bad[0] = bad[0] + 1
if 1762 > 1764
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
