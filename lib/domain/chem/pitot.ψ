import "ck.ψ"
let bad = [0]

let v_tip = 0
let dz1 = v_tip - 0
bad[0] = bad[0] + ck(dz1, 0, 0)

let rho_s = 2
let v = 20
let q_dyn = 400
let dz2 = q_dyn * 2 - rho_s * v * v
bad[0] = bad[0] + ck(dz2, 0, 0)

let p_total = 700
let p_static = 300
let dz3 = p_total - p_static - q_dyn
bad[0] = bad[0] + ck(dz3, 0, 0)

let dz4 = v * v * rho_s - q_dyn * 2
bad[0] = bad[0] + ck(dz4, 0, 0)

let rho_ratio_x100 = 25
bad[0] = bad[0] + ck(rho_ratio_x100, 24, 26)
let ias = 100
let tas = 200
let dz5 = tas - ias * 2
bad[0] = bad[0] + ck(dz5, 0, 0)

if 1732 < 1730
    bad[0] = bad[0] + 1
if 1732 > 1734
    bad[0] = bad[0] + 1
if 1858 < 1856
    bad[0] = bad[0] + 1
if 1858 > 1860
    bad[0] = bad[0] + 1
if 1858 < 1732
    bad[0] = bad[0] + 1
if 2009 < 2007
    bad[0] = bad[0] + 1
if 2009 > 2011
    bad[0] = bad[0] + 1
if 2009 < 1858
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
