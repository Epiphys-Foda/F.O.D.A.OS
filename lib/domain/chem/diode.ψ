import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let kT = 26
bad[0] = bad[0] + ck(kT, 24, 28)

let ex = small_div(600, 26)
bad[0] = bad[0] + ck(ex, 20, 26)

let i_s = 1
bad[0] = bad[0] + ck_hi(i_s, 5)

let i_forward = 1000000
if i_forward < i_s * 1000000
    bad[0] = bad[0] + 1

let lam_led = small_div(1240000, 1900)
bad[0] = bad[0] + ck(lam_led, 630, 670)

if 1947 < 1945
    bad[0] = bad[0] + 1
if 1947 > 1949
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
