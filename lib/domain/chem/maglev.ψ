import "ck.ψ"
let bad = [0]

let vmax = 603
bad[0] = bad[0] + ck(vmax, 580, 620)

let shm = 430
bad[0] = bad[0] + ck(shm, 400, 460)

let ems = 1
let eds = 0 - 1
let liftz = ems + eds
bad[0] = bad[0] + ck(liftz, 0, 0)

let gap = 10
bad[0] = bad[0] + ck(gap, 8, 15)

let pin = 1
bad[0] = bad[0] + ck_lo(pin, 1)

if 2015 < 2013
    bad[0] = bad[0] + 1
if 2015 > 2017
    bad[0] = bad[0] + 1
if 2003 < 2001
    bad[0] = bad[0] + 1
if 2003 > 2005
    bad[0] = bad[0] + 1
if 1970 < 1968
    bad[0] = bad[0] + 1
if 1970 > 1972
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
