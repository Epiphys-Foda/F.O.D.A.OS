import "ck.ψ"
let bad = [0]

let k = 24
let lam = 589
let dn = k * lam / 1000
bad[0] = bad[0] + ck(dn, 12, 16)

let dnp = dn
let dnm = dn
let dpar = dnp - dnm
bad[0] = bad[0] + ck(dpar, 0, 0)

let t0 = 0
let t1 = 50
let t2 = 100
bad[0] = bad[0] + ck_hi(t0, 0)
let dlad = t2 - 2 * t1
bad[0] = bad[0] + ck(dlad, 0, 0)

let spd = 1000000 / 1000
bad[0] = bad[0] + ck(spd, 900, 1100)

let moke = 5
bad[0] = bad[0] + ck(moke, 4, 20)
if 1875 < 1873
    bad[0] = bad[0] + 1
if 1875 > 1877
    bad[0] = bad[0] + 1
if 1893 < 1891
    bad[0] = bad[0] + 1
if 1893 > 1895
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
