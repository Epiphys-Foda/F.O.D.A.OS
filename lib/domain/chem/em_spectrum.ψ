import "ck.ψ"
let bad = [0]

let lam_r_km = 300
let f_r_MHz = 1
let dz1 = lam_r_km * f_r_MHz - 300
bad[0] = bad[0] + ck(dz1, 0, 0)

let lam_m_x100 = 12
let f_GHz = 2
let dz2 = lam_m_x100 * f_GHz - 24
bad[0] = bad[0] + ck(dz2, 0, 0)

let lam_g_nm = 550
let dz3 = lam_g_nm - 550
bad[0] = bad[0] + ck(dz3, 0, 0)

let lam_x_nm = 1
let dz4 = lam_x_nm - 1
bad[0] = bad[0] + ck(dz4, 0, 0)

let span = 14
bad[0] = bad[0] + ck(span, 12, 16)

if 1865 < 1863
    bad[0] = bad[0] + 1
if 1865 > 1867
    bad[0] = bad[0] + 1
if 1887 < 1885
    bad[0] = bad[0] + 1
if 1887 > 1889
    bad[0] = bad[0] + 1
if 1887 < 1865
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
