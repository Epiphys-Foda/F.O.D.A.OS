import "ck.ψ"
let bad = [0]

let rot_d = 665
bad[0] = bad[0] + ck(rot_d, 600, 700)

let rot_l = 0 - rot_d
let racz = rot_d + rot_l
bad[0] = bad[0] + ck(racz, 0, 0)

let opp = 1
bad[0] = bad[0] + ck_lo(opp, 1)

let homo = 1
bad[0] = bad[0] + ck_lo(homo, 1)

if 1815 < 1813
    bad[0] = bad[0] + 1
if 1815 > 1817
    bad[0] = bad[0] + 1
if 1848 < 1846
    bad[0] = bad[0] + 1
if 1848 > 1850
    bad[0] = bad[0] + 1
if 1961 < 1959
    bad[0] = bad[0] + 1
if 1961 > 1963
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
