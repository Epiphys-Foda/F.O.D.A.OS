import "ck.ψ"
let bad = [0]

let m_pi = 135
let f_pi = 92
let ainv2 = 18769
let m3 = m_pi * m_pi * m_pi
let f2 = f_pi * f_pi
let den1 = 1984 * f2
let den = den1 * ainv2
let num = m3 * 10000000
let g10 = num / den
bad[0] = bad[0] + ck(g10, 75, 79)

let tau = 6582 / g10
bad[0] = bad[0] + ck(tau, 80, 88)

let corr = 0
bad[0] = bad[0] + ck(corr, 0, 0)

let pump = 20
let bfac = 1
let b1 = 1
let b2 = 2
let m1 = pump * bfac * b1 * b1
let m2 = pump * bfac * b2 * b2
let rho1 = 0 - m1
let rho2 = 0 - m2
bad[0] = bad[0] + ck_hi(rho1, 0)
bad[0] = bad[0] + ck_hi(rho2, 0)
let ratio2 = m2 / m1
bad[0] = bad[0] + ck(ratio2, 4, 4)

if 1949 < 1947
    bad[0] = bad[0] + 1
if 1949 > 1951
    bad[0] = bad[0] + 1
if 1969 < 1967
    bad[0] = bad[0] + 1
if 1969 > 1971
    bad[0] = bad[0] + 1
if 2015 < 2013
    bad[0] = bad[0] + 1
if 2015 > 2017
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
