import "ck.ψ"
let bad = [0]

let vmax = 10
let km = 9
let s_low = 1
let s_mid = 9
let s_high = 81

let v_mid = vmax * s_mid / (km + s_mid)
let dz1 = v_mid * 2 - vmax
bad[0] = bad[0] + ck(dz1, 0, 0)

let v_low = vmax * s_low / (km + s_low)
let v_high = vmax * s_high / (km + s_high)
let dz2 = v_low - 1
bad[0] = bad[0] + ck(dz2, 0, 0)
let dz3 = v_high - 9
bad[0] = bad[0] + ck(dz3, 0, 0)

let dz4 = v_low + v_high - vmax
bad[0] = bad[0] + ck(dz4, 0, 0)

let s_2km = 18
let v_2km = vmax * s_2km / (km + s_2km)
let dz5 = v_2km - 6
bad[0] = bad[0] + ck(dz5, 0, 0)

if 1913 < 1911
    bad[0] = bad[0] + 1
if 1913 > 1915
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
