import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]
let e6 = 1000000
let e8 = e6 * 100

let nAtm = 25
bad[0] = bad[0] + ck(nAtm, 20, 30)

let nIsm = 1
if nIsm != 1
    bad[0] = bad[0] + 1

let nIgm = 1
if nIgm != 1
    bad[0] = bad[0] + 1

let eta10 = small_div(25 * 10000, 411)
bad[0] = bad[0] + ck(eta10, 580, 640)

if 25 < 1
    bad[0] = bad[0] + 1

let luxury = 6
bad[0] = bad[0] + ck(luxury, 5, 7)

arr_get(arr_new_n(1), bad[0])
