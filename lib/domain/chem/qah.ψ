import "ck.ψ"
let bad = [0]

let rxy = 25813
let drxy = rxy - 25813
bad[0] = bad[0] + ck(drxy, 0, 0)

let chern = 1
let dch = chern - 1
bad[0] = bad[0] + ck(dch, 0, 0)

let b_ext = 0
bad[0] = bad[0] + ck_hi(b_ext, 0)

let rxx = 0
bad[0] = bad[0] + ck_hi(rxx, 0)

if 1988 < 1986
    bad[0] = bad[0] + 1
if 1988 > 1990
    bad[0] = bad[0] + 1
if 2013 < 2011
    bad[0] = bad[0] + 1
if 2013 > 2015
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
