import "ck.ψ"
let bad = [0]

let hl_libby = 5568
let hl = 5730
bad[0] = bad[0] + ck(hl_libby, 5560, 5576)
bad[0] = bad[0] + ck(hl, 5700, 5760)
bad[0] = bad[0] + ck_lo(hl, hl_libby)

let n0 = 800
let n1 = 400
let n2 = 200
let n3 = 100
let dz1 = n1 * 2 - n0
bad[0] = bad[0] + ck(dz1, 0, 0)
let dz2 = n2 * 2 * 2 - n0
bad[0] = bad[0] + ck(dz2, 0, 0)
let dz3 = n3 * 2 * 2 * 2 - n0
bad[0] = bad[0] + ck(dz3, 0, 0)

let age1 = hl
let age2 = hl * 2
let age3 = hl * 3
bad[0] = bad[0] + ck(age1, 5700, 5760)
bad[0] = bad[0] + ck(age2, 11400, 11520)
bad[0] = bad[0] + ck(age3, 17100, 17280)

let age_oz = 5300
let act_oz = 53
bad[0] = bad[0] + ck(age_oz, 5100, 5500)
bad[0] = bad[0] + ck(act_oz, 50, 55)

let age_sh = 700
let act_sh = 92
bad[0] = bad[0] + ck(age_sh, 650, 750)
bad[0] = bad[0] + ck(act_sh, 90, 94)

let p10 = 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2
let dz4 = p10 - 1024
bad[0] = bad[0] + ck(dz4, 0, 0)
let limit = 10 * hl
bad[0] = bad[0] + ck(limit, 57000, 57600)

let prod = 2
bad[0] = bad[0] + ck(prod, 1, 3)
let rat = 12
bad[0] = bad[0] + ck(rat, 10, 15)

if 1949 < 1947
    bad[0] = bad[0] + 1
if 1949 > 1951
    bad[0] = bad[0] + 1
if 1960 < 1958
    bad[0] = bad[0] + 1
if 1960 > 1962
    bad[0] = bad[0] + 1
if 1988 < 1986
    bad[0] = bad[0] + 1
if 1988 > 1990
    bad[0] = bad[0] + 1
if 1991 < 1989
    bad[0] = bad[0] + 1
if 1991 > 1993
    bad[0] = bad[0] + 1
if 1988 < 1949
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
