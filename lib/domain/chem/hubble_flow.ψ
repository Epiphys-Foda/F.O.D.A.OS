import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let oldH = 464
bad[0] = bad[0] + ck(oldH, 430, 500)

let newH = 674
bad[0] = bad[0] + ck(newH, 660, 690)

let zRatio = small_div(674 * 100, 464)
bad[0] = bad[0] + ck(zRatio, 140, 150)

let v100 = small_div(674 * 1000, 100)
bad[0] = bad[0] + ck(v100, 6600, 6900)

let vZ = small_div(1 * 300000, 100)
bad[0] = bad[0] + ck(vZ, 2900, 3100)

let allRed = 1
if allRed != 1
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
