import "ck.ψ"
let bad = [0]

let m_kg = 1000
let h_m = 100
let e_block = m_kg * 10 * h_m
let dz1 = e_block - 1000000
bad[0] = bad[0] + ck(dz1, 0, 0)

let w_h = e_block / 3600
let dz2 = w_h - 277
bad[0] = bad[0] + ck(dz2, 0, 0)

let house_day = 10 * 3600000
let dz3 = house_day - 36000000
bad[0] = bad[0] + ck(dz3, 0, 0)

let hoist_kg = house_day / (10 * h_m)
let dz4 = hoist_kg - 36000
bad[0] = bad[0] + ck(dz4, 0, 0)

bad[0] = bad[0] + ck_lo(hoist_kg, 1000)

let bath_j = 30000000000000
let bath_m3 = bath_j / (10 * h_m) / 1000
let dz5 = bath_m3 - 30000000
bad[0] = bad[0] + ck(dz5, 0, 0)

if 1985 < 1983
    bad[0] = bad[0] + 1
if 1985 > 1987
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
