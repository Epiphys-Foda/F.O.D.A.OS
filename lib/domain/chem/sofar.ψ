import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let v2 = 2200000000 / 1000
let v_w = isqrt(v2)
bad[0] = bad[0] + ck(v_w, 1470, 1495)

let dtemp = small_div(16 * 24, 10)
bad[0] = bad[0] + ck(dtemp, 30, 45)

let dpress = small_div(1700, 100)
bad[0] = bad[0] + ck(dpress, 14, 20)

let axis = 1000
bad[0] = bad[0] + ck(axis, 800, 1300)

let v_min = 1490
bad[0] = bad[0] + ck(v_min, 1470, 1510)

let v_surf = 1530
bad[0] = bad[0] + ck_hi(v_min, v_surf)

let whale = 800
bad[0] = bad[0] + ck(whale, 500, 2000)

if 1948 < 1946
    bad[0] = bad[0] + 1
if 1948 > 1950
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
