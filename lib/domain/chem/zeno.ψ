import "ck.ψ"
let bad = [0]

let t10 = 10
let quad = t10 * t10
let dq = quad - 100
bad[0] = bad[0] + ck(dq, 0, 0)

let expd = 1000
let sup = expd / quad
bad[0] = bad[0] + ck(sup, 10, 10)

let nmeas = 10
let decay_n = nmeas * 1 * 1
let freeze = quad / decay_n
bad[0] = bad[0] + ck(freeze, 10, 10)

let zeno = 1
bad[0] = bad[0] + ck_lo(zeno, 1)

if 1954 < 1952
    bad[0] = bad[0] + 1
if 1954 > 1956
    bad[0] = bad[0] + 1
if 1977 < 1975
    bad[0] = bad[0] + 1
if 1977 > 1979
    bad[0] = bad[0] + 1
if 1990 < 1988
    bad[0] = bad[0] + 1
if 1990 > 1992
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
