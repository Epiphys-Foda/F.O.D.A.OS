import "ck.ψ"
let bad = [0]

let n1 = 100
let n2 = 200
let n3 = 400
let n4 = 800
let dz1 = n2 - n1 * 2
bad[0] = bad[0] + ck(dz1, 0, 0)
let dz2 = n3 - n2 * 2
bad[0] = bad[0] + ck(dz2, 0, 0)
let dz3 = n4 - n3 * 2
bad[0] = bad[0] + ck(dz3, 0, 0)

let n_steady = 100
let dz4 = n_steady - n1
bad[0] = bad[0] + ck(dz4, 0, 0)
let n_sub = 90
bad[0] = bad[0] + ck_hi(n_sub, n1)
bad[0] = bad[0] + ck_lo(n4, n1)

let nu_x10 = 24
bad[0] = bad[0] + ck(nu_x10, 23, 25)

let delayed = 65
bad[0] = bad[0] + ck(delayed, 60, 70)
bad[0] = bad[0] + ck_lo(delayed, 1)

let k_cp1 = 10006
bad[0] = bad[0] + ck(k_cp1, 10004, 10010)
let year_cp1 = 1942
bad[0] = bad[0] + ck(year_cp1, 1940, 1944)

let k_norm = 10
let k_void = 11
bad[0] = bad[0] + ck_lo(k_void, k_norm)
let year_c = 1986
bad[0] = bad[0] + ck(year_c, 1984, 1988)
bad[0] = bad[0] + ck_lo(year_c, year_cp1)

let coal_t = 3000
bad[0] = bad[0] + ck(coal_t, 2000, 4000)

if 1942 < 1940
    bad[0] = bad[0] + 1
if 1942 > 1944
    bad[0] = bad[0] + 1
if 1986 < 1984
    bad[0] = bad[0] + 1
if 1986 > 1988
    bad[0] = bad[0] + 1
if 1986 < 1942
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
