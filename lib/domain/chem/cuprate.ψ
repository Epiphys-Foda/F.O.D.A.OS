import "ck.ψ"
let bad = [0]

let ybco = 93
let ln2 = 77
bad[0] = bad[0] + ck(ybco, 90, 96)
bad[0] = bad[0] + ck(ln2, 75, 79)
bad[0] = bad[0] + ck_lo(ybco, ln2)

let peak = 95
let side = 10
bad[0] = bad[0] + ck_lo(peak, side)

let s1 = 1
let s2 = 0 - 1
let s3 = 1
let s4 = 0 - 1
let dsum = s1 + s2 + s3 + s4
bad[0] = bad[0] + ck(dsum, 0, 0)

let planes = 2
bad[0] = bad[0] + ck_lo(planes, 2)

if 1986 < 1984
    bad[0] = bad[0] + 1
if 1986 > 1988
    bad[0] = bad[0] + 1
if 1987 < 1985
    bad[0] = bad[0] + 1
if 1987 > 1989
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
