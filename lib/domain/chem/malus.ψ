import "ck.ψ"
let bad = [0]

let i0 = 100
let i45 = 50
let dz1 = i45 * 2 - i0
bad[0] = bad[0] + ck(dz1, 0, 0)

let i90 = 0
let dz2 = i90 - 0
bad[0] = bad[0] + ck(dz2, 0, 0)

let i_three = 25
let i_two = 0
let dz3 = i_three - i_two - 25
bad[0] = bad[0] + ck(dz3, 0, 0)
bad[0] = bad[0] + ck_lo(i_three, i_two)

let dz4 = i_three * 2 * 2 - i0
bad[0] = bad[0] + ck(dz4, 0, 0)

let i_unpol = 50
let dz5 = i_unpol * 2 - i0
bad[0] = bad[0] + ck(dz5, 0, 0)

if 1808 < 1806
    bad[0] = bad[0] + 1
if 1808 > 1810
    bad[0] = bad[0] + 1
if 1821 < 1819
    bad[0] = bad[0] + 1
if 1821 > 1823
    bad[0] = bad[0] + 1
if 1821 < 1808
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
