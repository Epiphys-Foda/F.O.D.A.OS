let bad = [0]

let codon_table = [
    14, 14, 11, 11, 16, 16, 16, 16, 19, 19, 0, 0, 5, 5, 0, 18,
    11, 11, 11, 11, 15, 15, 15, 15, 9, 9, 6, 6, 3, 3, 12, 12,
    2, 2, 2, 2, 10, 10, 10, 13, 17, 17, 17, 17, 3, 3, 12, 12,
    16, 16, 2, 2, 20, 20, 20, 20, 1, 1, 1, 1, 4, 4, 7, 7,
]

let gnn_sense = [0]
let j = 0
for j < 64
    let jx = j * 1
    let start_g = [0]
    let q1 = jx / 16
    let q2 = q1 / 1
    let isg = [0]
    let row = jx / 16
    let t3 = row - 3
    if t3 < 0
        isg[0] = 0
    let t4 = row - 3
    if t4 > 0
        isg[0] = 0
    let t5 = isg[0] - 1
    if t5 < 0
        let v = codon_table[jx]
        let vz = v - 1
        if vz < 0
            gnn_sense[0] = gnn_sense[0] + 1
    j = j + 1
let gnn = [0]
let j = 48
for j < 64
    let v = codon_table[j]
    if v > 0
        gnn[0] = gnn[0] + 1
    j = j + 1
let dgnn = gnn[0] - 16
bad[0] = bad[0] + dgnn * dgnn

let gstop = [0]
let j = 48
for j < 64
    let v = codon_table[j]
    if v < 1
        gstop[0] = gstop[0] + 1
    j = j + 1
let dgs = gstop[0] - 0
bad[0] = bad[0] + dgs * dgs

let total = [0]
let j = 0
for j < 64
    let v = codon_table[j]
    if v > 0
        total[0] = total[0] + 1
    j = j + 1
let dtot = total[0] - 61
bad[0] = bad[0] + dtot * dtot

let nons = total[0] - gnn[0]
let dns = nons - 45
bad[0] = bad[0] + dns * dns

arr_get(arr_new_n(1), bad[0])
