import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let kAmp = 57
bad[0] = bad[0] + ck(kAmp, 50, 65)

let pOrb = 42
bad[0] = bad[0] + ck(pOrb, 40, 45)

let dz = small_div(5700000, 300000)
bad[0] = bad[0] + ck(dz, 18, 20)

let dLam = small_div(500 * 57000, 300000)
bad[0] = bad[0] + ck(dLam, 90, 110)

let nExo = 5500
bad[0] = bad[0] + ck(nExo, 5000, 6000)

arr_get(arr_new_n(1), bad[0])
