import "ck.ψ"
let bad = [0]

let t1 = 25
let t2 = 20
bad[0] = bad[0] + ck(t1, 20, 30)
bad[0] = bad[0] + ck(t2, 15, 25)

let topo = 0
if t2 > t1
    topo = 1
bad[0] = bad[0] + ck_lo(topo, 0)

let wind = topo
bad[0] = bad[0] + ck(wind, 0, 1)

if 1979 < 1977
    bad[0] = bad[0] + 1
if 1979 > 1981
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
