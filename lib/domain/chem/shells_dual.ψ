let bad = [0]

let sn = [
    1, 2, 2, 3, 3, 4, 3, 4, 5, 4,
    5, 6, 4, 5, 6, 7, 5, 6, 7,
]

let sl = [
    0, 0, 1, 0, 1, 0, 2, 1, 0, 2,
    1, 0, 3, 2, 1, 0, 3, 2, 1,
]

let nob = [2, 10, 18, 36, 54, 86, 118]
let nuc = [2, 8, 20, 28, 50, 82, 126, 184]

let cum = [0]
let cum = [0]
let j = 0
let nobx = [0]
for j < 19
    let jx = j * 1
    let l = sl[jx]
    let cap = 4 * l + 2
    cum[0] = cum[0] + cap
    let ql = l - 1
    let aql =
        if ql < 0
            0 - ql
        else
            ql
    if aql < 1
        nobx[0] = nobx[0] + 1
    j = j + 1
let dn = nobx[0] - 6
bad[0] = bad[0] + dn * dn
let g1 = cum[0] - cum[0]
bad[0] = bad[0] + g1 * g1
let nm = 2 + 8 + 20 + 28 + 50 + 82 + 126 + 184 - 500
bad[0] = bad[0] + nm * nm
let en = 2 + 10 + 18 + 36 + 54 + 86 + 118 - 324
bad[0] = bad[0] + en * en

arr_get(arr_new_n(1), bad[0])
