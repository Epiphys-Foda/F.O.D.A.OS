import "ck.ψ"
let bad = [0]

let sm = 5
let mach = 2
let jz = sm * mach - 10
bad[0] = bad[0] + ck(jz, 0, 0)

let over = 100
bad[0] = bad[0] + ck(over, 50, 200)

let m20 = 20
bad[0] = bad[0] + ck(m20, 19, 22)

let cke = 26
bad[0] = bad[0] + ck(cke, 20, 30)

let after = 1
bad[0] = bad[0] + ck_lo(after, 1)

if 1842 < 1840
    bad[0] = bad[0] + 1
if 1842 > 1844
    bad[0] = bad[0] + 1
if 1887 < 1885
    bad[0] = bad[0] + 1
if 1887 > 1889
    bad[0] = bad[0] + 1
if 1969 < 1967
    bad[0] = bad[0] + 1
if 1969 > 1971
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
