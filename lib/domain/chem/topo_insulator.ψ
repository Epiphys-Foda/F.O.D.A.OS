import "ck.ψ"
let bad = [0]

let tin = 1
bad[0] = bad[0] + ck_lo(tin, 1)

let nu = 2
let dnu = nu - 2
bad[0] = bad[0] + ck(dnu, 0, 0)
let edge = nu * nu / nu
let dedge = edge - nu
bad[0] = bad[0] + ck(dedge, 0, 0)

let pair = 2
let dpr = pair - 2
bad[0] = bad[0] + ck(dpr, 0, 0)

let lock = 180
bad[0] = bad[0] + ck(lock, 170, 190)

if 1982 < 1980
    bad[0] = bad[0] + 1
if 1982 > 1984
    bad[0] = bad[0] + 1
if 1988 < 1986
    bad[0] = bad[0] + 1
if 1988 > 1990
    bad[0] = bad[0] + 1
if 2016 < 2014
    bad[0] = bad[0] + 1
if 2016 > 2018
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
