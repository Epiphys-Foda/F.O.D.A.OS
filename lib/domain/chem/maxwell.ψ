import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let mu0 = 126
let eps0 = 885
bad[0] = bad[0] + ck(mu0, 120, 132)
bad[0] = bad[0] + ck(eps0, 850, 900)

let c_light = 300000
bad[0] = bad[0] + ck(c_light, 290000, 310000)

let gamma_wl = 1
let radio_wl = 1000
bad[0] = bad[0] + ck_lo(radio_wl, gamma_wl)

let c_radio = 300000
if c_radio != c_light
    bad[0] = bad[0] + 1

if 1865 < 1863
    bad[0] = bad[0] + 1
if 1865 > 1867
    bad[0] = bad[0] + 1
if 1887 < 1885
    bad[0] = bad[0] + 1
if 1887 > 1889
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
