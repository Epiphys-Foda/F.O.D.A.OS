import "ck.ψ"
let bad = [0]

let mol_in = 4
let mol_out = 2
let dz1 = mol_in - mol_out * 2
bad[0] = bad[0] + ck(dz1, 0, 0)

let yield_low = 15
let yield_high = 40
bad[0] = bad[0] + ck_lo(yield_high, yield_low)
if yield_high < yield_low * 2
    bad[0] = bad[0] + 1

let cat_factor = 1
bad[0] = bad[0] + ck_lo(cat_factor, 1)

let t_shift = 1
let q_in = 100
let dz3 = q_in - 100
bad[0] = bad[0] + ck(dz3, 0, 0)

if 1884 < 1882
    bad[0] = bad[0] + 1
if 1884 > 1886
    bad[0] = bad[0] + 1
if 1918 < 1916
    bad[0] = bad[0] + 1
if 1918 > 1920
    bad[0] = bad[0] + 1
if 1918 < 1884
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
