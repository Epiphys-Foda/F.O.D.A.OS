import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let eV3 = 195
bad[0] = bad[0] + ck(eV3, 190, 200)

let kT = 26
let dE = 1960
let boltz = small_div(kT * 100, dE)
bad[0] = bad[0] + ck_hi(boltz, 10)

let inv = 1
bad[0] = bad[0] + ck_lo(inv, 0)

let n_out = 2
if n_out != 2
    bad[0] = bad[0] + 1

let clone = 1
if clone != 1
    bad[0] = bad[0] + 1

let coh = 300
bad[0] = bad[0] + ck(coh, 250, 350)

if 1960 < 1958
    bad[0] = bad[0] + 1
if 1960 > 1962
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
