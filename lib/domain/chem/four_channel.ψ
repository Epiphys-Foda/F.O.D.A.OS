import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let cert = [0]
let t1 = smod(13606 * 1002, 65537)
cert[0] = smod(cert[0] + t1, 65537)
let t2 = smod(500 * 716, 65537)
cert[0] = smod(cert[0] + t2, 65537)
let t3 = smod(782 * 1366, 65537)
cert[0] = smod(cert[0] + t3, 65537)
let t4 = smod(0 * 1926, 65537)
cert[0] = smod(cert[0] + t4, 65537)

let dv = cert[0] - 51451
let adv = smod(dv + 65537, 65537)
bad[0] = bad[0] + adv

arr_get(arr_new_n(1), bad[0])
