import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]
let e5 = 100000

let omega5 = small_div(62832 * 10, 864)
bad[0] = bad[0] + ck(omega5, 725, 730)

let sin45 = isqrt(5 * 100000)
bad[0] = bad[0] + ck(sin45, 706, 708)

let aC = 2 * omega5 * 10 * sin45 / 1000
bad[0] = bad[0] + ck(aC, 10200, 10400)

let sin0 = 0
if sin0 != 0
    bad[0] = bad[0] + 1

let sinS = 0 - 707
let sinN = 707
if sinS + sinN != 0
    bad[0] = bad[0] + 1

let minLat = 5
bad[0] = bad[0] + ck(minLat, 4, 7)

arr_get(arr_new_n(1), bad[0])
