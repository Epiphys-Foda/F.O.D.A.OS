import "ck.ψ"
let bad = [0]

let k315 = 315
let k3 = k315 * k315 * k315
let exact = 31250000
let dev = k3 - exact
let devr = 0 - dev
bad[0] = bad[0] + ck_hi(dev, 625000)
bad[0] = bad[0] + ck_hi(devr, 625000)

let v126 = 126
bad[0] = bad[0] + ck(v126, 120, 130)

let re_t = 2300
bad[0] = bad[0] + ck(re_t, 2000, 3000)

let l_mm = 1000
let eta_mm = 1
let prod = eta_mm * 1000
let deta = prod - l_mm
bad[0] = bad[0] + ck(deta, 0, 0)

let scale_sep = 1000000
bad[0] = bad[0] + ck(scale_sep, 500000, 2000000)

let down = 1
bad[0] = bad[0] + ck_lo(down, 1)

if 1922 < 1920
    bad[0] = bad[0] + 1
if 1922 > 1924
    bad[0] = bad[0] + 1
if 1941 < 1939
    bad[0] = bad[0] + 1
if 1941 > 1943
    bad[0] = bad[0] + 1
if 1962 < 1960
    bad[0] = bad[0] + 1
if 1962 > 1964
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
