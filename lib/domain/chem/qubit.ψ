import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let fq = 5000
bad[0] = bad[0] + ck(fq, 4000, 6000)

let t_op = 10
bad[0] = bad[0] + ck(t_op, 5, 20)

let kT_cold = 1
let e_photon = 200
bad[0] = bad[0] + ck_hi(kT_cold, e_photon)

let t1 = 100
bad[0] = bad[0] + ck(t1, 50, 200)

let gates = small_div(t1 * 1000, 20)
bad[0] = bad[0] + ck(gates, 3000, 7000)

let err = 1
bad[0] = bad[0] + ck_hi(err, 5)

if 1962 < 1960
    bad[0] = bad[0] + 1
if 1962 > 1964
    bad[0] = bad[0] + 1
if 1999 < 1997
    bad[0] = bad[0] + 1
if 1999 > 2001
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
