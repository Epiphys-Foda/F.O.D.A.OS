import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let g10 = 10
let h_deep = 4000
let v2_deep = g10 * h_deep
let v_deep = isqrt(v2_deep)
bad[0] = bad[0] + ck(v_deep, 180, 220)

let h_shore = 10
let v2_shore = g10 * h_shore
let v_shore = isqrt(v2_shore)
bad[0] = bad[0] + ck(v_shore, 8, 12)

bad[0] = bad[0] + ck_hi(v_shore, v_deep)

let amp_ratio = 45
bad[0] = bad[0] + ck(amp_ratio, 30, 60)

let buoy = 100
bad[0] = bad[0] + ck(buoy, 50, 200)

if 2004 < 2002
    bad[0] = bad[0] + 1
if 2004 > 2006
    bad[0] = bad[0] + 1
if 1946 < 1944
    bad[0] = bad[0] + 1
if 1946 > 1948
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
