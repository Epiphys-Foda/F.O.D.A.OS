import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let den = 4 * 31416 * 16
let dden = den - 2010624
bad[0] = bad[0] + dden * dden

let vR = 0
let eR = 1492078 * den - 3000000000000
if eR < 0
    vR = vR + 1
let fR = 1492079 * den - 3000000000000
if fR < 1
    vR = vR + 1
bad[0] = bad[0] + vR * vR

let r0c3 = 1492078
let num = r0c3 * 1000
let r0x = icbrt(num)
let dr = r0x - 1142
bad[0] = bad[0] + dr * dr

let t4 = 1200 - r0x
let a4 =
    if t4 < 0
        0 - t4
    else
        t4
let w4 = a4 - 80
let ww4 =
    if w4 < 0
        0
    else
        w4
bad[0] = bad[0] + ww4

arr_get(arr_new_n(1), bad[0])
