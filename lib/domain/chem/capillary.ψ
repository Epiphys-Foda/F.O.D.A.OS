import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let gam = 728
bad[0] = bad[0] + ck(gam, 720, 735)

let h = small_div(1456000, 4905)
bad[0] = bad[0] + ck(h, 280, 310)

let hx = small_div(1456000, 9810)
bad[0] = bad[0] + ck(hx, 140, 155)

let redwood = 10000
bad[0] = bad[0] + ck(redwood, 9000, 12000)

bad[0] = bad[0] + ck_hi(hx, redwood)

let w = small_div(123 * 981, 100000)
let s = small_div(2 * gam * 2, 1000)
bad[0] = bad[0] + ck_lo(s, w)

let hot = 55
bad[0] = bad[0] + ck_hi(hot, gam)

arr_get(arr_new_n(1), bad[0])
