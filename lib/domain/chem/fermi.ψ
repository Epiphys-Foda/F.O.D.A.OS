import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let ef = 70
bad[0] = bad[0] + ck(ef, 65, 75)

let vf = 1000000
bad[0] = bad[0] + ck_hi(vf, 1500000)

let p_deg = 380
bad[0] = bad[0] + ck(p_deg, 300, 450)

let kt = 26
let fraction = small_div(kt, ef * 100)
bad[0] = bad[0] + ck_hi(fraction, 1)

let wd = 360
bad[0] = bad[0] + ck(wd, 300, 400)

if 1925 < 1923
    bad[0] = bad[0] + 1
if 1925 > 1927
    bad[0] = bad[0] + 1

if 1926 < 1924
    bad[0] = bad[0] + 1
if 1926 > 1928
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
