import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let lc = 2426
bad[0] = bad[0] + ck(lc, 2400, 2450)

let s90 = lc
if s90 != lc
    bad[0] = bad[0] + 1

let s180 = lc * 2
if s180 != s90 * 2
    bad[0] = bad[0] + 1

bad[0] = bad[0] + ck_lo(s180, s90)

if 1923 < 1921
    bad[0] = bad[0] + 1
if 1923 > 1925
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
