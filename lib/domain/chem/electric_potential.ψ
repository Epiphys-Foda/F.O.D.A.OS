import "ck.ψ"
let bad = [0]

let k_e = 9
let q_uc = 1
let r_m = 3
let v_pot = k_e * q_uc * 1000 / r_m
let dz1 = v_pot - 3000
bad[0] = bad[0] + ck(dz1, 0, 0)

let volt_d = 12
let q_c = 2
let work = volt_d * q_c
let dz2 = work - 24
bad[0] = bad[0] + ck(dz2, 0, 0)

let v1 = 9000
let v2 = 9000
let v_sum = v1 + v2
let dz3 = v_sum - 18000
bad[0] = bad[0] + ck(dz3, 0, 0)

let ev_j = 2
bad[0] = bad[0] + ck(ev_j, 1, 3)

if 1800 < 1798
    bad[0] = bad[0] + 1
if 1800 > 1802
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
