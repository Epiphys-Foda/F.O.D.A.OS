import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let dB = small_div(10000, 314)
bad[0] = bad[0] + ck(dB, 30, 34)

let dM = small_div(10000, 286)
bad[0] = bad[0] + ck(dM, 33, 36)

let ly10 = small_div(dM * 3262, 1000)
bad[0] = bad[0] + ck(ly10, 110, 118)

let dA = small_div(10000, 74)
bad[0] = bad[0] + ck(dA, 130, 140)

let baseline = 200
if baseline == 0
    bad[0] = bad[0] + 1

let pcAu = 206265
bad[0] = bad[0] + ck(pcAu, 206000, 206500)

arr_get(arr_new_n(1), bad[0])
