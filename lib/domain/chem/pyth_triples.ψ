import "ck.ψ"
let bad = [0]

let a1 = 2 * 2 * 1
let b1 = 2 * 2 - 1 * 1
let c1 = 2 * 2 + 1 * 1
let dz1 = a1 * a1 + b1 * b1 - c1 * c1
bad[0] = bad[0] + ck(dz1, 0, 0)

let a2 = 2 * 3 * 2
let b2 = 3 * 3 - 2 * 2
let c2 = 3 * 3 + 2 * 2
let dz2 = a2 * a2 + b2 * b2 - c2 * c2
bad[0] = bad[0] + ck(dz2, 0, 0)

let a3 = 2 * 4 * 1
let b3 = 4 * 4 - 1 * 1
let c3 = 4 * 4 + 1 * 1
let dz3 = a3 * a3 + b3 * b3 - c3 * c3
bad[0] = bad[0] + ck(dz3, 0, 0)

let a4 = 2 * 4 * 3
let b4 = 4 * 4 - 3 * 3
let c4 = 4 * 4 + 3 * 3
let dz4 = a4 * a4 + b4 * b4 - c4 * c4
bad[0] = bad[0] + ck(dz4, 0, 0)

let a5 = 2 * 5 * 2
let b5 = 5 * 5 - 2 * 2
let c5 = 5 * 5 + 2 * 2
let dz5 = a5 * a5 + b5 * b5 - c5 * c5
bad[0] = bad[0] + ck(dz5, 0, 0)

if 300 < 295
    bad[0] = bad[0] + 1
if 300 > 305
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
