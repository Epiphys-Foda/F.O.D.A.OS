import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let mu_B = 93
bad[0] = bad[0] + ck(mu_B, 88, 98)

let n_beams = 2
if n_beams != 2
    bad[0] = bad[0] + 1

let g_dirac = 2
if g_dirac != 2
    bad[0] = bad[0] + 1

let a_e = 116
bad[0] = bad[0] + ck(a_e, 110, 120)

bad[0] = bad[0] + ck_lo(a_e, 0)

let zeeman = 93
bad[0] = bad[0] + ck(zeeman, 85, 100)

if 1922 < 1920
    bad[0] = bad[0] + 1
if 1922 > 1924
    bad[0] = bad[0] + 1
if 1928 < 1926
    bad[0] = bad[0] + 1
if 1928 > 1930
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
