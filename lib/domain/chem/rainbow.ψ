import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let nw = 1330
let nd = 2420
bad[0] = bad[0] + ck(nw, 1320, 1340)
bad[0] = bad[0] + ck(nd, 2400, 2440)

let sc = small_div(1000 * 1000, nw)
bad[0] = bad[0] + ck(sc, 740, 760)
bad[0] = bad[0] + ck_hi(sc, 1000)

let tcw = 488
bad[0] = bad[0] + ck(tcw, 480, 495)

let scd = small_div(1000 * 1000, nd)
bad[0] = bad[0] + ck(scd, 405, 420)

let tcd = 244
bad[0] = bad[0] + ck(tcd, 240, 250)

bad[0] = bad[0] + ck_hi(tcd, tcw)

let ap = small_div(200 * 100, 133)
bad[0] = bad[0] + ck(ap, 145, 155)

let s45 = isqrt(500000)
bad[0] = bad[0] + ck(s45, 700, 712)

let s2 = small_div(s45 * 1000, nw)
bad[0] = bad[0] + ck(s2, 525, 538)

let t2 = 321
bad[0] = bad[0] + ck(t2, 315, 328)

let red = 424
let violet = 406
let width = red - violet
bad[0] = bad[0] + ck(width, 15, 21)

let dn = 13
bad[0] = bad[0] + ck(dn, 10, 16)

let sec = 510
bad[0] = bad[0] + ck(sec, 505, 515)
bad[0] = bad[0] + ck_lo(sec, red)

if 1637 < 1635
    bad[0] = bad[0] + 1
if 1637 > 1640
    bad[0] = bad[0] + 1
if 1666 < 1664
    bad[0] = bad[0] + 1
if 1666 > 1668
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
