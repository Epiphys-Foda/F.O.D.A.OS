import "ck.ψ"
let bad = [0]

let beta_num = 3
let dz1 = beta_num * beta_num - 9
bad[0] = bad[0] + ck(dz1, 0, 0)
let dz2 = 9 + 16 - 25
bad[0] = bad[0] + ck(dz2, 0, 0)

let g_num = 5
let g_den = 4
let dz3 = g_num * 4 - g_den * 5
bad[0] = bad[0] + ck(dz3, 0, 0)

let age_ship = 8
let age_earth = 10
let dz4 = age_ship * 5 - age_earth * 4
bad[0] = bad[0] + ck(dz4, 0, 0)
bad[0] = bad[0] + ck_hi(age_ship, age_earth)

let turns = 1
let stays = 0
bad[0] = bad[0] + ck_lo(turns, 1)
bad[0] = bad[0] + ck_hi(stays, turns)

if 1905 < 1903
    bad[0] = bad[0] + 1
if 1905 > 1907
    bad[0] = bad[0] + 1
if 1971 < 1969
    bad[0] = bad[0] + 1
if 1971 > 1973
    bad[0] = bad[0] + 1
if 1971 < 1905
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
