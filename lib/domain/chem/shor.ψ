import "ck.ψ"
let bad = [0]

let p1 = 49 - 3 * 15
let dp1 = p1 - 4
bad[0] = bad[0] + ck(dp1, 0, 0)
let sq = p1 * p1
let p2 = sq - 15
let dp = p2 - 1
bad[0] = bad[0] + ck(dp, 0, 0)

let m1 = 48 - (48 / 15) * 15
let m2 = 15 - (15 / m1) * m1
let g1 = m1 - 3
bad[0] = bad[0] + ck(g1, 0, 0)

let m3 = 50 - (50 / 15) * 15
let m4 = 15 - (15 / m3) * m3
let g2 = m3 - 5
bad[0] = bad[0] + ck(g2, 0, 0)

let prod = 3 * 5
let dprod = prod - 15
bad[0] = bad[0] + ck(dprod, 0, 0)

if 1994 < 1992
    bad[0] = bad[0] + 1
if 1994 > 1996
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
