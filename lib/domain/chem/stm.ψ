import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let sq = isqrt(40000)
let kappa = 512 * sq / 1000 / 10
bad[0] = bad[0] + ck(kappa, 9, 12)

let sens = 74
bad[0] = bad[0] + ck(sens, 70, 78)

let res = 10
bad[0] = bad[0] + ck(res, 5, 50)

let cur = 1
bad[0] = bad[0] + ck_lo(cur, 1)

if 1981 < 1979
    bad[0] = bad[0] + 1
if 1981 > 1983
    bad[0] = bad[0] + 1
if 1986 < 1984
    bad[0] = bad[0] + 1
if 1986 > 1988
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
