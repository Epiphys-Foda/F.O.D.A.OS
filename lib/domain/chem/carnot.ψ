import "ck.ψ"
import "../../core/stdlib/numtheor.ψ"

let bad = [0]

let etaWater = small_div(300 * 1000, 600)
bad[0] = bad[0] + ck(etaWater, 480, 520)

let etaHe = small_div(700 * 1000, 1000)
bad[0] = bad[0] + ck(etaHe, 680, 720)

let etaCoal = small_div(510 * 1000, 810)
bad[0] = bad[0] + ck(etaCoal, 610, 650)

bad[0] = bad[0] + ck_hi(etaWater, etaHe)

let iterE = small_div(500 * 50, 100)
bad[0] = bad[0] + ck(iterE, 240, 260)

if 600 < 300
    bad[0] = bad[0] + 1

arr_get(arr_new_n(1), bad[0])
