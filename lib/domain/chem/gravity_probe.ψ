import "ck.ψ"
let bad = [0]

let num = 6674 * 279759076
let q = num / 5 / 6323
let dq = q - 59057791
bad[0] = bad[0] + dq * dq

let q2 = (num / 12 / 2) / 8011
let dq2 = q2 - 9711189
bad[0] = bad[0] + dq2 * dq2

if 5000000 > q2
    bad[0] = bad[0] + 1
bad[0] = bad[0] + ck_hi(q2, 20000000)

arr_get(arr_new_n(1), bad[0])
