let str_bytes(s) = s

let chars(s) = s

let str_concat(a, b) =
    let out = []
    let i = 0
    for i < len(a)
      out = out.push(a[i])
      i = i + 1
    let j = 0
    for j < len(b)
      out = out.push(b[j])
      j = j + 1
    out

let is_int(x) =
    (x < 256 ? 1 : 0)

let flat(a) =
    let out = []
    let i = 0
    for i < len(a)
      let e = a[i]
      if is_int(e)
        out = out.push(e)
      else
        let j = 0
        for j < len(e)
          out = out.push(e[j])
          j = j + 1
        0
      i = i + 1
    out

let slice(a, i, j) =
    let out = []
    let n = len(a)
    let s0 = (i < 0 ? 0 : i)
    let s1 = (j > n ? n : j)
    let k = s0
    for k < s1
      out = out.push(a[k])
      k = k + 1
    out

let contains(a, sub) =
    let found = false
    let n = len(a)
    let m = len(sub)
    let i = 0
    let d = false
    for !d
      if i <= n - m
        let matched = true
        let j = 0
        for j < m
          if a[i + j] != sub[j]
            matched = false
          else
            0
          j = j + 1
        if matched
          found = true
          d = true
        else
          i = i + 1
      else
        d = true
    (found ? 1 : 0)

let starts_with(a, pre) =
    let ok = true
    let n = len(a)
    let m = len(pre)
    if m > n
      ok = false
    else
      0
    let i = 0
    let d = false
    for !d
      if i < m
        if ok
          if a[i] != pre[i]
            ok = false
            d = true
          else
            i = i + 1
        else
          d = true
      else
        d = true
    (ok ? 1 : 0)

let str(v) =
    let out = []
    if v == 0
      out = out.push(48)
    else
      0
    let x = v
    if v < 0
      out = out.push(45)
      x = 0 - x
    else
      0
    let digits = []
    for x > 0
      let d = x - (x / 10) * 10
      digits = digits.push(48 + d)
      x = x / 10
    let k = len(digits) - 1
    for k >= 0
      out = out.push(digits[k])
      k = k - 1
    out

let index_of(a, v) =
    let found = -1
    let i = 0
    let d = false
    for !d
      if i < len(a)
        if (found < 0) * (a[i] == v)
          found = i
        else
          0
        i = i + 1
      else
        d = true
    found

let str_eq(a, b) =
    let eq = 0
    if a - b
    else
      eq = 1
      0
    if eq
    else
      if a < 256
        if b < 256
        else
          if len(b) - 1
          else
            if a - b[0]
            else
              eq = 1
      else
        if b < 256
          if len(a) - 1
          else
            if b - a[0]
            else
              eq = 1
        else
          0
        0
      0
    if eq
    else
      if a < 256
      else
        if b < 256
        else
          if len(a) - len(b)
          else
            if len(a) < 16384
              let i = 0
              let d = false
              for !d
                if i < len(a)
                  if a[i] - b[i]
                    d = true
                  else
                    i = i + 1
                else
                  d = true
              if i >= len(a)
                eq = 1
              else
                  0
            else
              0
            0
          0
        0
      0
    eq

let concat(a, b) = str_concat(a, (b < 256 ? [b] : b))

let read_file_bytes(path) =
    let n = host_call(10, path, 0)
    let out = []
    let i = 0
    let d = false
    for !d
      if i < n
        out = out.push(host_call(4, i, 0))
        i = i + 1
      else
        d = true
    out
